# 開發進度

更新時間：2026-09-12

## 目前完成到哪個模組

1. ✅ 資金流向 — TWSE OpenAPI (T86)，端點：`POST/GET /api/institutional-flow`
2. ✅ 籌碼面 — TWSE OpenAPI (MI_MARGN)，端點：`POST/GET /api/chip-data`
3. ✅ 大戶持股比例 — TDCC 集保結算所開放資料，端點：`POST/GET /api/shareholding`
4. ✅ 股票產業分類 — TWSE 上市公司基本資料，端點：`POST /api/stocks/industry/fetch`
5. ✅ 意見領袖模組 — 7 位（Chris／Mr.Market市場先生／股乾爹／柴鼠兄弟／郭哲榮分析師／
   老王愛說笑／游庭皓的財經皓角），涵蓋 Threads／YouTube／部落格三種來源
6. ✅ 新聞模組 — Yahoo 股市 RSS + LLM 篩選個股相關性
7. ✅ 「今日多空共識」首頁 — 依產業別統計意見領袖／新聞的多空分布，端點：`GET /api/consensus`
8. ✅ 排程自動化 — APScheduler，平日盤後自動抓資金流向/籌碼面/意見領袖/新聞，週六抓大戶持股
9. ✅ React 前端 — 首頁預設顯示「今日多空共識」，個股查詢降級為次要功能（「查詢單一股票細節」）
10. ✅ `start.bat` / `stop.bat` — 雙擊即可啟動/關閉前後端，不用再透過終端機手動操作

## 下一步

CLAUDE.md 定案的四大方向（資金流向、籌碼面、意見領袖、新聞）+ 大戶持股比例都已經有 MVP，
首頁也重新設計成「一眼看出多空共識」的儀表板。可以考慮的後續方向：
- 產業分類目前是一次性手動抓取，之後可視需要排入週期排程
- YouTube 意見領袖只有標題可用（無影片簡介全文），分類準確度受限，若要提升需要接入字幕/逐字稿
- 免責聲明／法遵文字：使用者已確認純個人使用，暫不需要額外處理
- 「今日多空共識」目前是 7 天滾動視窗，如果之後資料量變多，可以評估縮短視窗或做每日快照

## 這次 session 的關鍵技術決策

- **大戶持股比例的「大戶」定義**：TDCC 集保戶股權分散表官方分級代碼 15（1,000,001股以上，
  即 1,000 張以上），代碼 17 才是「合計」列（代碼 16 是保留欄位、官網不會顯示、永遠是 0）——
  這點容易搞混，已經用 2330 實測數字（84.82%、1485人）跟官網核對過。
- **產業分類**：TWSE OpenAPI `t187ap03_L`（上市公司基本資料）裡的「產業別」是兩位數代碼
  （例如 24=半導體業），對照表是官方固定的 38 類（`isin.twse.com.tw/isin/class_i.jsp?kind=1`
  查到的），07、34 這兩個代碼本來就沒有用，不是漏抓。
- **「今日多空共識」用 7 天滾動視窗，不是嚴格的「今天」**：因為意見領袖/新聞不是每天都有
  新內容，抓「今日」很可能常常整頁空白，改用滾動視窗＋清楚標示天數，比較符合實際可用性。
- **意見領袖分類也加上 `is_stock_relevant` 過濾**（原本只有新聞模組有）：因為部落格/YouTube
  來源常常混雜非個股內容（例如市場先生的銀行帳戶開箱文、股乾爹的總經閒聊標題），沒有這層
  過濾會讓「今日多空共識」被雜訊稀釋。
- **YouTube 用官方頻道 RSS**（`youtube.com/feeds/videos.xml?channel_id=...`），不需要
  API key，但只有影片標題沒有簡介全文，分類依據比 Threads/部落格薄弱，這是資料源本身的
  限制、非工程問題。
- **四個舊 router（資金流向/籌碼面/意見領袖/新聞）的抓取邏輯抽到 `app/ingest.py`**，
  讓手動按鈕（router）跟排程（scheduler.py）共用同一套邏輯，不用維護兩份。
- **SQLite 沒有正式 migration 工具**，加新欄位（例如 `stocks.industry`）時
  `Base.metadata.create_all()` 不會幫舊資料庫補欄位，寫了一個輕量的 `ensure_columns()`
  在啟動時自動 `ALTER TABLE` 補齊，之後每加一個新欄位要記得在那裡補一行。
- **`start.bat`/`stop.bat` 不能用中文訊息**：中文字元在 cmd.exe 剖析批次檔時，卡在
  `(` `)` `%` 這些語法符號附近會讓整個指令碼解析錯亂（甚至誤觸發到 Windows 內建的
  `AT` 排程指令），改成純英文訊息才正常，這是 Windows 批次檔的已知地雷。

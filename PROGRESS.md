# 開發進度

更新時間：2026-09-15

## 目前完成到哪個模組

1. ✅ 資金流向 — TWSE OpenAPI (T86)
2. ✅ 籌碼面 — TWSE OpenAPI (MI_MARGN)
3. ✅ 大戶持股比例 — TDCC 集保結算所
4. ✅ 股票產業分類 — TWSE 上市公司基本資料
5. ✅ 意見領袖模組 — 7 位，Threads／YouTube／部落格三種來源
6. ✅ 新聞模組 — Yahoo 股市 RSS
7. ✅ 「今日多空共識」首頁 — 依產業＋依個股統計，個股有分歧標示
8. ✅ 個股共識摘要卡 — 查詢單一股票時最上方顯示看多/看空/中性統計
9. ✅ 意見領袖個人頁 — 依股票分組的完整發言紀錄，標記立場「延續」或「反轉」
10. ✅ 排程自動化 — APScheduler
11. ✅ start.bat / stop.bat
12. ✅ 視覺改版 — 兩欄排版減少滾動、卡片加陰影/hover、配色改成接近 Claude 的
    暖色調（米白背景＋陶土橘主色）
13. ✅ 主力資金流向（依產業）— 三大法人買賣超依產業加總排行，跟意見領袖/新聞
    的主觀多空共識是不同資料源，首頁分開呈現
14. ✅ 資料日期顯示修正 — 資金流向/籌碼面/大戶持股改用純日期（不再顯示假的
    00:00），「重新整理」按鈕修正為會一併刷新主力資金流向
15. ✅ 主力資金流向可調整天數 — 下拉選單（當日／3／5／10／20 個交易日），
    預設「當日」
16. ✅ 大戶持股比例排行（依產業）— 跟主力資金流向同樣的橫條圖呈現方式；
    只有一週快照時顯示「目前比例」，累積兩週以上會自動切換成「週對週增減」
17. ✅ 產業點擊看類股列表、個股改用懸浮視窗 — 點產業彈出該產業所有股票；
    點個股（不管從哪裡點）都彈出視窗顯示完整細節，不用再滾到頁面底部
18. ✅ 左側快捷導覽列 — 固定在畫面左邊，7 個區塊點了直接跳轉

這次是照著使用者要求「更接近 Capafy Alpha Consensus」的方向做的四項功能優化，
之後追加的一次視覺改版（排版/卡片/配色），「主力資金流向」這項新功能，
以及這次 session 的體驗優化（日期顯示、彈窗、左側導覽、大戶持股排行）。

## 下一步

四大方向＋大戶持股都有 MVP，功能面跟 Capafy 原版對齊過，視覺也重新設計過。可能的後續：
- Capafy 有日/週/月三種時間維度報告，目前只有一個可調整天數的滾動視窗，
  之後可以做成三個預設按鈕
- 意見領袖個人頁目前沒有「整體準確度」，Capafy 也刻意不做這個（強調觀點演變
  而非對錯），維持現狀即可
- 產業分類、大戶持股都還是手動/半自動抓取，排程涵蓋範圍可以再擴大
- 配色是憑印象刻出來的暖色調，不是逐像素比對 Claude 官方設計稿，之後可再微調

## 這次 session 的關鍵技術決策

- **日期型欄位改回傳 `date` 而不是硬轉成 `datetime`**：資金流向/籌碼面/大戶持股
  本來就是「日」為單位，之前用 `datetime.combine(d, time.min)` 硬轉，前端顯示出
  一個假的「00:00」看起來像半夜更新的。改成 schema 直接宣告 `date | None`，
  前端 `formatUpdatedAt` 判斷字串裡有沒有 `T` 來決定要不要顯示到分鐘。
- **抓出「重新整理」按鈕的真正 bug**：按鈕原本只呼叫 `loadConsensus()`，沒呼叫
  `loadIndustryFlow()`，導致「主力資金流向」永遠不會被這顆按鈕更新到。統一
  改成 `refreshHome()` 一次刷新所有首頁資料源，所有抓取按鈕也一併改用。
- **股票細節從「頁面下方 inline 區塊」改成「彈出視窗」**：抽成獨立的
  `StockDetailModal` 元件，自己管理 loading/error/資料 state，不再需要在
  `App.jsx` 裡維護一大包 per-stock state；不管從哪裡點股票（熱門個股橫條圖、
  產業類股列表、手動搜尋）都開同一個元件，行為一致。
- **產業/個股點擊統一用單一 `modal` state 判斷型別**（`{type:'stock',...}` 或
  `{type:'industry',...}`），點擊子項目時直接「替換」而非「疊加」——例如從
  產業類股列表點進某檔股票後，關閉視窗會直接回首頁，不會退回產業列表。這是
  刻意簡化的取捨，之後如果需要「上一頁」行為可以再改成用 stack。
- **大戶持股排行有兩種模式（`level`／`change`）**：TDCC 資料每週才更新，
  資料庫只有一週快照時沒辦法算「週對週變化」，所以後端會自動判斷目前有幾週
  資料，只有一週就回傳「目前比例排行」，兩週以上才回傳「增減變化」，前端
  元件也對應兩種畫法，不需要額外設定。
- **這次 session 截圖工具持續失效（畫面看起來空白）**，但用 `find`／
  `read_network_requests` 直接查 DOM 內容跟 API 回應都確認資料正確——純粹是
  截圖時機問題，之後如果又遇到同樣狀況，先用這兩個方式確認而不是照單全收。

## 更早之前的技術決策（仍然有效）

- **「主力資金流向」用「最近 N 個實際有資料的交易日」而非日曆天數**：直接
  `SELECT DISTINCT date ... ORDER BY date DESC LIMIT N`，確保拿到的是真的
  N 個交易日，不會被週末/假日稀釋成只有 3 天資料。
- **跟「多空共識」明確區分成兩個獨立區塊**：一個是三大法人買賣超的真實金額
  （硬資料），一個是意見領袖/新聞的主觀情緒統計（軟資料），資料性質不同，
  混在一起容易讓使用者誤以為是同一件事。
- **用 CSS variables 集中管理配色**（`:root` 裡的 `--bg`、`--accent`、`--bullish`、
  `--bearish` 等），之後要再調色只要改一個地方，不用到處找 hex code。
- **用 `.grid-2col` + `.scroll-panel` 兩個共用 class 解決滾動過長問題**：並排
  兩欄的地方用 grid，內容量不固定又容易很長的地方（意見/新聞 feed）用固定
  高度＋`overflow-y: auto`，避免單一區塊把整頁撐爆。
- **依 Capafy 官方頁面實地考察後才動手**（功能面優化）：沒有憑印象猜測，用
  WebFetch 抓了 Capafy Alpha Consensus 的產品頁面確認實際設計（個股共識、
  分歧標示、交易者卡片、觀點演變追蹤、日週月報告），再對照現有落差排優先順序。
- **分歧判斷邏輯很單純**：同一檔股票的意見/新聞裡，只要同時出現「看多」和
  「看空」（用同一套 bullish/bearish 正規化），就標記 has_divergence。
- **立場反轉判斷只比較「前一次」，不是「整體趨勢」**：同一意見領袖對同一檔
  股票，最新一次的多空分類跟前一次不同就是「反轉」，這是簡化版，沒有處理
  「連續反轉又反轉回來」這種更複雜的模式判斷。
- **`_collect_items` 抽成共用函式**：首頁的「今日多空共識」跟單一股票的
  「共識摘要卡」共用同一套「撈意見+新聞、依股票/產業分組統計」邏輯，只是
  filter 條件（有沒有指定 stock_id）不同，避免兩份幾乎一樣的程式碼。
- **大戶持股比例的「大戶」定義**：TDCC 集保戶股權分散表官方分級代碼 15（1,000,001股以上，
  即 1,000 張以上），代碼 17 才是「合計」列（代碼 16 是保留欄位、官網不會顯示、永遠是 0）。
- **產業分類**：TWSE OpenAPI `t187ap03_L` 裡的「產業別」是兩位數代碼（例如 24=半導體業），
  對照表是官方固定的 38 類，07、34 這兩個代碼本來就沒有用，不是漏抓。
- **意見領袖分類也加上 `is_stock_relevant` 過濾**：避免部落格/YouTube 的非個股內容
  （銀行開箱文、總經閒聊標題）稀釋多空共識的準確度。
- **YouTube 用官方頻道 RSS**，不需要 API key，但只有標題沒有簡介全文，分類依據較薄弱。
- **抓取邏輯集中在 `app/ingest.py`**，router（手動按鈕）跟 `scheduler.py`（排程）共用。
- **SQLite 沒有正式 migration 工具**，加新欄位要在 `ensure_columns()` 補一行手動 `ALTER TABLE`。
- **`start.bat`/`stop.bat` 只能用純英文訊息**，中文字元會讓 cmd.exe 剖析批次檔時整個解析錯亂。
